[build]
  command = "npx @11ty/eleventy"
  publish = "_site"

[build.environment]
  NODE_VERSION = "20"

# Serve pretty URLs and a branded 404
[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404
